# Source of all gems in the project used. They can be installed using bundle install
source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "cucumber"
gem "bundler"
gem "rest-client"
gem "json"
gem "test-unit"
gem "minitest/autorun"
gem "report_builder"
